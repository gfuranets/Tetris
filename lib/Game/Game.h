#pragma once

#include "Grid.h"
#include "Tetromino.h"

class Game
{
private:
    Grid grid;
    Tetromino current;

public:
    // 1. main game loop
    // 3. input managing
    // 4. spawning pieces
    // 5. detecting game over
    // 6. timing + gravity
    // 7. render contents (managed by esp32)
};
